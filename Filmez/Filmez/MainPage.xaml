<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Filmez.MainPage">

    <StackLayout Orientation="Vertical" Padding="0,15">
        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" Padding="10,0,0,0" >
            <Label x:Name="LabelName" FontSize="22" TextColor="DarkGray" />
        </StackLayout>
        <ListView x:Name="listViewMovies" ItemsSource="{Binding}" CachingStrategy="RecycleElement" 
                  ItemSelected="ListView_ItemSelected" SeparatorVisibility="Default" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding Title}" Detail="{Binding Release_date}" ImageSource="{Binding Image}" TextColor="Purple" DetailColor="#503026"/>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>



</ContentPage>
